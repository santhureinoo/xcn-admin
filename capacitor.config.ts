import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.ingamecurrencyplatform',
  appName: 'in-game-currency-platform-react',
  webDir: 'build'
};

export default config;
